fastapi==0.115.0
uvicorn[standard]==0.24.0
sqlalchemy==2.0.35
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
cryptography>=41.0.0  # Explicit for PHI encryption (Fernet)
python-multipart==0.0.12
pydantic-settings==2.6.0
python-dotenv==1.0.1
alembic==1.13.1
aiofiles==24.1.0
pillow>=10.4.0
email-validator>=2.0.0
httpx>=0.27.0

# Scheduling and timezone
APScheduler>=3.10.0
pytz>=2024.1

# SMS (Twilio)
twilio>=8.0.0

# Numpy - required for sentence-transformers, chromadb, etc.
numpy>=1.24.0,<2.0.0

# PDF/OCR extraction (text-first approach)
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
paddleocr>=2.7.0
paddlepaddle>=2.6.0

# RAG and LLM
langchain>=0.1.0
langchain-community>=0.0.10
chromadb>=0.4.0
sentence-transformers>=2.2.0
ollama>=0.1.0
google-generativeai>=0.4.0

# Local MedGemma via Hugging Face
transformers>=4.56.0
# CPU-only torch to avoid pulling CUDA/NVIDIA wheels.
# Note: Some PyTorch CPU indexes don't publish a `+cpu` local version tag for older releases,
# so pin the plain version.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0
accelerate>=0.30.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# Memory layer (Mem0)
mem0ai>=0.1.0

# Knowledge graph (Graphiti with Gemini support)
graphiti-core[google-genai]>=0.5.0

# Neo4j driver (required by both Mem0 and Graphiti)
neo4j>=5.26.0
